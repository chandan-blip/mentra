# Mentra app (app.mentradev.sbs) — the product itself, behind a login.
# Marketing content lives on the landing host (https://mentradev.sbs) and serves
# its own robots.txt + sitemap; rules here do NOT apply to it.
#
# Deny by default: only the sign-in root and static brand assets are worth
# indexing. Every other path is a login-gated SPA route, so new routes are
# covered automatically instead of needing a line added here.

User-agent: *
Allow: /$
Allow: /favicon.png
Allow: /og-image.png
Allow: /manifest.webmanifest
Allow: /brand/
Allow: /assets/
Disallow: /

Sitemap: https://app.mentradev.sbs/sitemap.xml
